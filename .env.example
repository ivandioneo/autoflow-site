# Activepieces webhook URL — rotate this if it is ever exposed in git history
# Copy to .env.local and set the real value. Never commit .env.local.
VITE_WEBHOOK_URL=https://automate.ivanit.work/api/v1/webhooks/REPLACE_ME
